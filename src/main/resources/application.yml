spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/my_igbc_db_1
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  servlet:
    context-path: /awards
debug: true
